# Cases To Consider During Analysis

Escolher uma entrada para considerar quando analizarmos um algoritimo pode ter um impacto significativo em omo um algoritmo vai performar 

Por exemplo: se a lista de entrada já está ordenada algum algoritmo de ordenação vai performar muito bem, mas outro algoritmo pode performar porcamente. O oposto aconteceria também

## (A) Best Case Input

Isso representa o conjunto de entrada que permite um algoritmo performar mais rapidamente. Com essa entrada o algoritmo tomararia um tempo mais curto 